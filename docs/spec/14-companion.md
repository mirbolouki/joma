# ۱۴ — جوجهٔ من

> **قرارداد سند مادر:** `۱۰٫۶` تا `۱۰٫۱۰` · `PD-008` (بازگشت با استقبال) · `CAR-01` (بی‌پاداشی) · `CAR-04` (جوجه ارزش اصلی نیست) · `WTR-10` (پیش‌نویس آب پاداش نمی‌سازد) · `17٫۲` (حریم خصوصی)
> **تصمیم مالک (قفل‌شده):** جوجه = **جوجهٔ جغد** — نه جوجهٔ مرغ زرد · تناسب **الف: بدنِ گردِ یکپارچه** · «انیمیشنی کامل و بامزه با صدا» · کاربر باید به آن **دلبسته** شود.

---

## ۰) داستان کامل جوجه — یک‌جا، برای تأیید نهایی

> این بخش، **روایت یکپارچه** است. بخش‌های ۱ تا ۱۱ بعد از آن، همان داستان را با جدول و کد و ریزقاعده می‌گویند.
> اگر جایی از این روایت با بقیهٔ سند اختلاف داشت، **این روایت حاکم است** — چون مالک روی آن حرف می‌زند.

### پردهٔ ۱ — از کجا آمد

کاربر تازه ثبت‌نام می‌کند. هنوز هیچ برنامه‌ای ندارد، هیچ ثبتی نکرده. در «جوجهٔ من» **یک تخم** می‌بیند: سفید کرم، با سه لکهٔ ریز، و یک پیام: «هر قدم تو، یک دونه برای این تخم کوچولوست 🌱».
هیچ قفلی نیست، هیچ شرطی نیست، هیچ «برای دیدن جوجه، اول ۷ روز ثبت کن» وجود ندارد. تخم همان‌جاست و منتظر است.

**سه احساسی که این قابلیت می‌سازد** (`۱۰٫۶`): تعلق («این همراه کوچک بخشی از مسیر من است») · تداوم («ثبت‌های من در طول زمان معنا پیدا می‌کنند») · پیشرفت ملموس («توجه من به روزهایم اثرِ دیدنی دارد»).
**و یک خط قرمز که از همان پردهٔ اول مشخص است:** جوجه **ارزش اصلی محصول نیست**. چرخهٔ اصلی جوما «برنامه → زندگی واقعی → ثبت صادقانه → الگو → برنامهٔ بهتر» است؛ جوجه فقط کنار این چرخه می‌ایستد.

### پردهٔ ۲ — تخم، ترک، تولد

با هر **ثبت معتبر**، یک دونه به ظرف دونه اضافه می‌شود. سنجه‌ها کم‌کم پر می‌شوند و تخم **می‌لرزد**:
«تخم می‌لرزد! ادامه بده، دارم می‌آیم… 🐣»
پس از رسیدن به آستانه — که **از بک‌اند می‌آید** و فرانت هیچ عدد ثابتی ندارد — ترک می‌خورد و بعد **متولد می‌شود**.
🔴 **تولد فقط یک بار است.** در دموی 7B بعد از هر ۱۲ واحد، تولد **تکرار** می‌شد؛ اینجا نه: بعد از تولد، جوجه **رشد** می‌کند. دلیلم ساده است: تولد دوباره، معجزه را به یک حلقهٔ تکراری تبدیل می‌کند و ارزش احساسی را می‌کشد.

### پردهٔ ۳ — این جوجه کیست

جوجهٔ مرغ زرد **حذف شد** — حس یک مرغ معمولی را می‌داد. جوجه، **جوجهٔ جغد** است، هم‌خانوادهٔ جغدِ محصول: بدنِ **گردِ یکپارچه** (سر و بدن یک فرم)، دو کاکل کوتاه، شکم کرم، و **حلقهٔ چشم طلایی** `#E8B64A` که امضای جغدِ جوماست.

| عنصر | رنگ |
|---|---|
| تن (سر و بدن) | `#5E93C8` |
| کاکل و بال‌ها | `#3F6FA0` |
| شکم | `#F6EBD9` · خط `#E7D9C2` |
| حلقهٔ چشم | `#E8B64A` |
| مردمک | `#2E3A48` + نقطهٔ سفید |
| منقار و پا | `#F0A63C` |
| لپ | `#E07898` (۵۰٪ شفاف) — **فقط در حالت شاد** |

**عینک طلایی همیشه روی صورت است** — در **همهٔ شش حالت**، حتی وقتی چشم‌ها بسته‌اند. این عینک، شناسنامهٔ بصری اوست. در حالت شاد که چشم‌ها کمانی می‌شوند، عینک کمی بزرگ‌تر و بالاتر می‌رود تا تلاقی نکند (در نمونهٔ زنده همین‌طور پیاده شده).

### پردهٔ ۴ — چهار سنجهٔ مراقبت

جوجه چهار چیز می‌خواهد و **هیچ‌کدام با کلیک پر نمی‌شود**:

| سنجه | با چه پر می‌شود | هرگز با چه |
|---|---|---|
| 🌾 ظرف دونه | ثبت معتبر کارهای روزانه | کلیک روی صفحه |
| 💧 ظرف آب | **آبِ قطعی‌شده** | پیش‌نویس آب (`WTR-10`) |
| 🏠 خونهٔ تمیز | ثبت معتبر حالِ ۵ قدمی | بازکردن ویزارد |
| 🛁 وقت حمام | ثبت ذخیره‌شده + تمرین تنفسِ انجام‌شده | زدن دکمهٔ تنفس |

هر سنجه سه بخش دارد (۳×۴ = ۱۲) و همهٔ اعداد از بک‌اند می‌آید؛ فرانت هیچ‌کدام را کم یا زیاد نمی‌کند.

### پردهٔ ۵ — شش حالت، همه از داده

| حالت | شرط (از بک‌اند) | چه می‌بینی |
|---|---|---|
| **آرام** | روز عادی | نفس آرام، پلک هر چند ثانیه |
| **شاد** | بعد از یک **ثبت معتبر** | چشمِ خندان، پرش کوتاه، جرقه، لپ‌های صورتی |
| **خواب** | شب یا چند روز بی‌ثبت | چشم بسته، «z»، بدن آرام‌تر |
| **نوازش** | لمس کاربر | تکان کوچک و گرم + صدا |
| **کم‌رنگ** | ۳ تا ۵ روز بی‌ثبت | پالت روشن‌تر، بی‌حرکت |
| **خاکستری** | ۶ روز یا بیشتر | پالت خاکستری، افتاده |

**کم‌رنگ و خاکستری، تنها نشانگر «نیازمند توجه»اند** — تصویری، بی‌عدد، بی‌سرزنش. هیچ‌جا نوشته نمی‌شود «۳ روز نیامدی».

### پردهٔ ۶ — صدا: جیک، خرخر، خروپف

| صدا | کجا | چطور ساخته شده |
|---|---|---|
| **جیک دو نُتی** | لمس | دو سینوس `۴۷۰→۷۸۰Hz` و `۶۲۰→۹۰۰Hz` با لرزش ۲۲Hz |
| **خرخر نوازش** | «نوازش» | موج مثلثی `۱۱۲→۱۲۶Hz` + ترمولوی ۲۶Hz + لایهٔ نفس |
| **خروپف نرم** | «بخوابانش» | نویز فیلترشده + سینوس بم؛ دم کوتاه، بازدم بلندتر |
| **خمیازه** | لحظهٔ خواب | سینوس نزولی `۳۴۰→۱۹۰Hz` |

🔴 **پیش‌فرض خاموش** — جوجه با ورود به صفحه صدا نمی‌زند. کلید مستقل «صدای جوجه» در همان کارت. **صدا پاداش نیست**: خرخر و جیک فقط واکنش‌اند و هیچ سنجه‌ای را پر نمی‌کنند.

### پردهٔ ۷ — لمس، بی‌پاداش

روی جوجه که بزنی: تکان، حباب کوتاه با **یک جملهٔ تصادفی** («چی چی!» · «خوشحالم که هستی.» · «امروز هم منتظرت بودم.»)، و صدای آرام.
**هیچ امتیاز، هیچ دونه، هیچ رشدی.** اگر پشت‌سرهم بزنی، واکنش‌ها **آرام‌تر** می‌شوند (`petGate`) — تا لمس به یک حلقهٔ بی‌معنی تبدیل نشود.

### پردهٔ ۸ — غیبت و بازگشت

کاربر چند روز نمی‌آید. جوجه **نمی‌میرد، بیمار نمی‌شود، فرار نمی‌کند** — فقط کم‌رنگ و بعد خاکستری می‌شود.
وقتی برمی‌گردد: «خوش برگشتی. جوجه منتظرت بود.» — **بدون شمارش روزهای غیبت**، بدون «جبران کن»، بدون راهنمای اجباری، و سوابق قبلی سرجایش. کاهش هرگز صفر نمی‌کند و جوجه را به تخم برنمی‌گرداند.

### پردهٔ ۹ — اسم

بعد از تولد، یک بار: «حالا که به دنیا آمده، اسمش را چه بگذاریم؟» · ۲ تا ۱۶ حرف · یک بار دیگر هم می‌شود عوض کرد («مطمئنی؟»).
اگر نامی نگذارد، «جوجهٔ من» می‌ماند. **نام فقط در همین صفحه دیده می‌شود**، نه در کارت خانه و نه جای دیگر. (مخزن نام: بک‌اند — وگرنه بین دستگاه‌ها فرق می‌کند.)

### پردهٔ ۱۰ — تاریخچه، که هرگز پاک نمی‌شود

```
🥚  ۳ شهریور   —  تخم گذاشته شد
🥚  ۱۷ شهریور  —  تخم ترک خورد
🐣  ۱۹ شهریور  —  به دنیا آمد
```
حتی اگر همهٔ سنجه‌ها خالی شوند، این خط زمانی می‌ماند: کاربر همیشه می‌بیند چقدر مسیر آمده (`PD-008`).

### پردهٔ ۱۱ — رشد

پس از تولد، یک نوار رشد پر می‌شود و مرحلهٔ بعدی از بک‌اند می‌آید. **هیچ قابلیت اصلیِ محصول پشت رشد جوجه قفل نمی‌شود** (`CAR-04`) — نه گزارش، نه کتابخانه، نه هم‌مسیر.

### پردهٔ ۱۲ — حریم خصوصی

وضعیت جوجه **با همراه به اشتراک گذاشته نمی‌شود**، مگر روزی که مالک صریحاً تصمیم بگیرد (`17٫۲`). جوجه، اتاق شخصی کاربر است.

---

### قواعد سرخ — به هیچ قیمتی

مرگ/بیماری/فرار · شمارش معکوس غیبت · هر جملهٔ سرزنش‌آمیز («گرسنه ماندم») · پاداش برای کلیک · پاداش برای پیش‌نویس آب · قفل‌کردن قابلیت پشت رشد · مقایسه با جوجهٔ دیگران · جوجهٔ مرغی زرد یا هر سبک خارج از خانوادهٔ جغد · پاداش‌دادن با صدا · اشتراک‌گذاری وضعیت با همراه بدون تصمیم صریح · تبدیل جوجه به ارزش اصلی محصول.

### چه چیزی از بک‌اند می‌آید (فهرست اتصال)

`stage` (تخم/ترک/جوجه) · `state` یکی از شش حالت · چهار سنجه و بخش‌هایشان · آستانهٔ تولد و مراحل رشد · `petName` · تاریخ‌های مهرها · پرچم «آب پیش‌نویس دارد» (تا سنجهٔ آب عوض نشود). **فرانت هیچ‌کدام را نمی‌سازد و هیچ حالتی را از روی کلیک استنتاج نمی‌کند.**

### 🔒 تصمیم‌های نهایی جوجه — **قفل شد** `[نسخهٔ ۱۷]`

> مالک: «بریم سناریوی جوجه را کامل کنیم و تمام.» هر شش مورد با پیشنهاد کارشناسی **نهایی** شد.
> تغییر هرکدام فقط با **نام‌بردن صریح مالک** (قانون قفل، `00-principles.md`).

| # | موضوع | تصمیم نهایی | وضعیت |
|---|---|---|---|
| ۱ | آستانهٔ تولد و مراحل رشد | **از بک‌اند**؛ فرانت هیچ عدد ثابتی ندارد. پیش‌فرض نمونه: تولد در **۱۲ واحد**، سپس مراحل رشد | ✅ در نمونه: نوار رشد + مراحل |
| ۲ | شدت کاهش در غیبت | **۳ تا ۵ روز ک‌م‌رنگ** · **۶ روز یا بیشتر خاکستری** — **هرگز صفر و هرگز برگشت به تخم** | ✅ نمونه: شش حالت |
| ۳ | آستانهٔ `petGate` | **۴ لمس در یک دقیقه:** فقط خرخر، بدون حباب · **۸ لمس:** فقط یک پلک | ✅ نمونه: پیاده شد |
| ۴ | مرحله‌های رشد | **سه مرحلهٔ نام‌دار:** «جوجهٔ کوچک» → «نوپا» → «بالغ» · آستانه از بک‌اند؛ اگر بک‌اند یک مرحله داد، همان یک نوار می‌ماند | ✅ نمونه: کلید «رشد» در نوار بازبینی |
| ۵ | ذخیرهٔ نام جوجه | **در بک‌اند** — وگرنه بین دستگاه‌ها فرق می‌کند. «یک بار گذاشتن + یک بار عوض‌کردن» | ✅ نمونه: پس از بار دوم، دکمهٔ تغییر می‌رود |
| ۶ | وضعیت جوجه برای هم‌مسیر | **پیش‌فرض: نه.** فقط با تصمیم صریح مالک — جوجه، اتاق شخصی کاربر است | ✅ نمونه: یادداشت حریم در صفحه |

### ✅ چک‌لیست کامل‌شدن — پرده‌های داستان در نمونه

| پرده | در نمونه دیده می‌شود؟ | کجا |
|---|---|---|
| ۱ تخم از روز اول | ✅ | صفحهٔ ۷ — حالت «تخم» بدون قفل |
| ۲ تخم، ترک، تولد (یک‌بار) | ✅ | کلید «جوجه: تخم / ترک / متولد» |
| ۳ هویت بصری (جغد، عینک طلایی در همهٔ حالت‌ها) | ✅ | شش حالت با عینک؛ لپ فقط در شادی |
| ۴ چهار سنجهٔ مراقبت | ✅ | کارت «مراقبت امروز» (۳×۴) |
| ۵ شش حالت از داده | ✅ | دکمه‌های حالت + شرط هر حالت |
| ۶ صدا (جیک/خرخر/خروپف/خمیازه) — پیش‌فرض خاموش | ✅ | کلید «صدای جوجه» |
| ۷ لمس بی‌پاداش + `petGate` | ✅ | «نوازش» — ۴ و ۸ لمس |
| ۸ غیبت و بازگشت بدون سرزنش | ✅ | حالت کم‌رنگ/خاکستری + متن بازگشت |
| ۹ نام (۲ تا ۱۶ حرف، یک بار تغییر) | ✅ | کارت نام + «مطمئنی؟» |
| ۱۰ تاریخچه که هرگز پاک نمی‌شود | ✅ | `.tline` — حتی با دادهٔ خالی |
| ۱۱ رشد، بدون قفل‌کردن قابلیت‌ها | ✅ | نوار رشد + `CAR-04` |
| ۱۲ حریم خصوصی | ✅ | یادداشت «اتاق شخصی توست» |

---

## ۱) دو هویت جدا — مهم‌ترین اصل این صفحه

| هویت | نقش | کجا دیده می‌شود |
|---|---|---|
| **جغد جوما** | راهنما، گوینده، بخشی از برند — **همان جغد مرجع، متحرک** (`01-foundation.md` §۲٫۴٫۱) | همه‌جای رابط |
| **جوجهٔ من** | همراه شخصی **کاربر** | فقط این صفحه + کارت کوچک در خانه |

**این دو قاطی نمی‌شوند.** جوجه حرف نمی‌زند، توصیه نمی‌دهد و بینش نمی‌سازد. جوجه **وضعیت** است، نه راهنما. (`۱۰٫۵`)

**`[تصمیم]`** در این صفحه **یک** پیام از جغد جوما می‌آید (راهنما)، و **جوجه خودش هیچ پیامی نمی‌دهد**؛ وضعیتش از نوار مراقبت و متن کوتاه خوانده می‌شود.

---

## ۲) ساختار صفحه

```
[۱] سرصفحه: نام و مرحله
[۲] صحنهٔ بزرگ جوجه (۲۳۰px)
[۳] چهار سنجهٔ مراقبت
[۴] نوار پیشرفت رشد
[۵] نام‌گذاری (یک‌بار)
[۶] پیام کوتاه جوما
[۷] تاریخچهٔ کوچک مراحل
```

---

## ۳) صحنهٔ جوجه — `.pet-stage`

```css
.pet-stage { width:100%; height:230px; display:grid; place-items:center;
  background: linear-gradient(160deg, var(--lav-soft), var(--sky-soft));
  border-radius: var(--r-lg); cursor:pointer; user-select:none; transition: transform .25s }
.pet-stage svg { width:150px; height:168px }
.pet-stage:hover { transform: translateY(-4px) }
.pet-stage.pop    { animation: pop .4s }
.pet-stage.wiggle { animation: wiggle .9s }
```

**نسخهٔ فشرده (کارت خانه):** `118×128` با `svg 82×92`.

### ۳.۱ مرحلهٔ ۱ — تخم `.egg`

```svg
<ellipse cx="43" cy="54" rx="29" ry="35" fill="#FFF7EA" stroke="#E5D5B4" stroke-width="2.5"/>
<ellipse cx="32" cy="42" rx="6" ry="10" fill="#fff" opacity=".8" transform="rotate(-22 32 42)"/>
```

### ۳.۲ مرحلهٔ ۲ — ترک `.crack`

همان تخم + گروه ترک با `stroke="#C9B387" stroke-width="2"`:

```svg
<path d="M30 60l6-5 4 6 6-4 5 5 6-4"/>
<path d="M40 74l5-4 4 5"/>
```

`.crack` فقط وقتی `show` می‌گیرد که **ترک شروع شده**.

### ۳.۳ مرحلهٔ ۳ — جوجهٔ جوما (جوجهٔ جغد) — **قفل‌شده**

> ### ✅ تصمیم مالک (این دور)
> «جوجه‌ی زرد حس یک جوجهٔ مرغ معمولی را دارد» ⇒ **جوجهٔ مرغ حذف شد.**
> «جوجهٔ جغد انیمیشنی کامل و بامزه باشد و صدا داشته باشد؛ کاربر به آن دلبسته شود.»
> «انتخاب من: **الف** — بدنِ گردِ یکپارچه.» ⇒ **طراحی قفل شد.**

**`[تصمیم]` هویت:** جوجه، **جوجهٔ جغد** است — هم‌خانوادهٔ جغدِ ۷B، نه سبک جدید و نه شکل دیگر.

| عنصر | رنگ | یادداشت |
|---|---|---|
| تن / سر و بدن | `#5E93C8` | همان آبی جغد محصول |
| کاکل و بال‌ها | `#3F6FA0` | دو کاکل کوتاه روی سر |
| شکم | `#F6EBD9` · خطِ شکم `#E7D9C2` | فرم کرمِ جغد |
| حلقهٔ چشم | `#E8B64A` | **امضای جغد جوما** |
| مردمک | `#2E3A48` + نقطهٔ سفید | چشم درشتِ بچگانه |
| منقار / پا | `#F0A63C` | — |
| لپ | `#E07898` با شفافیت ۵۰٪ | فقط در حالت‌های شاد |

**`[تصمیم]` تناسب (طرح الف):** بدنِ **گردِ یکپارچه**؛ سر و بدن یک فرم‌اند. در اندازهٔ کوچک (کارت خانه، `۸۲px`) خوانا می‌ماند.
README: **ممنوع** — جوجهٔ مرغی زرد · کاکل بلند · چشم‌های بی‌حلقه · سبک متفاوت با جغد.

**شش حالت — همه از دادهٔ بک‌اند، هیچ‌کدام از کلیک:**

| حالت | شرط (از بک‌اند) | نمایش |
|---|---|---|
| **آرام** | عادی | نفسِ آرام ۳٫۲s · پلک هر چند ثانیه |
| **شاد** | پس از یک **ثبت معتبر** | چشم‌های خندان · پرش کوتاه · جرقه |
| **خواب** | شب یا پس از چند روز بی‌ثبت | چشم بسته · «z» · بدن آرام‌تر |
| **نوازش** | لمس (بی‌پاداش) | تکان کوچک · خرخر |
| **کم‌رنگ** (`calm`) | ۳ تا ۵ روز بی‌ثبت | پالت روشن‌تر · بی‌حرکت |
| **خاکستری** (`miss`) | ۶ روز یا بیشتر | پالت خاکستری · افتاده — همان نشانگر «نیازمند توجه» |

> ### ✅ قاعدهٔ عینک و لپ — تأیید نهایی
>
> | قاعده | تصمیم |
> |---|---|
> | **عینک طلایی** | **در همهٔ شش حالت روی صورت است** — حتی چشم‌بسته (امضای بصری جوجه). در حالت «شاد» که چشم‌ها کمانی‌اند، عینک کمی بزرگ‌تر و بالاتر می‌رود تا با کمان‌ها تلاقی نکند |
> | **لپ صورتی** | **فقط در حالت «شاد»** — نه در حالت آرام، نه در خواب، نه در کم‌رنگ/خاکستری |
> | دلیل | عینک، شناسهٔ ثابتِ اوست؛ اگر در یک حالت بیفتد، به‌نظر خطای رندر می‌آید. لپ اما زبان حال است: یعنی «الان خوشحالم» |
>
> **در نمونهٔ زنده پیاده و بازبینی‌شده:** شش حالت در صفحهٔ «جوجهٔ من» با دکمهٔ بازبینی، و شرط هر حالت زیر صحنه نوشته می‌شود.

**`[تصمیم]` تخم و ترک:** تخم `#FFF7EA` با خط `#E5D5B4` و سه لکهٔ ریز؛ ترک فقط وقتی `show` می‌گیرد که ترک شروع شده باشد (همان دو مسیر قبلی، بدون تغییر).

### ۳.۳٫۱ صدا — جوجهٔ جغد (تصمیم مالک: «صدا داشته باشد»)

| صدا | کجا | ساخته‌شده با | مشخصات |
|---|---|---|---|
| **جیکِ دو نُتی** | لمس جوجه | دو سینوس + لرزش `۲۲Hz` | `۴۷۰→۷۸۰Hz` سپس `۶۲۰→۹۰۰Hz` |
| **خرخرِ نوازش** | دکمهٔ «نوازش» | مثلثی با ترمولوی `۲۶Hz` + فیلتر پایین‌گذر + لایهٔ نفس | `۱۱۲→۱۲۶Hz` · بالاگذر نفس `۹۰۰Hz` · «جیکِ» ریز پایانی |
| **خروپفِ نرم** | دکمهٔ «بخوابانش» | نویزِ فیلترشده + سینوسِ بم | دم `۰٫۶۲s` کوتاه · بازدم `۰٫۸۵s` بلندتر و بم‌تر · تکرار آرام |
| **خمیازه** | لحظهٔ خواب | سینوسِ نزولی | `۳۴۰→۱۹۰Hz` — و بعد خروپف |

**قواعد صدا:** پیش‌فرض **خاموش** تا تصمیم مالک دربارهٔ صدای پیش‌فرض · کلید مستقل «صدای جوجه» · در تب پنهان خاموش · هرگز روی بارگذاری صفحه صدا نمی‌زند · لمس پشت‌سرهم، صدا را **آرام‌تر** می‌کند (`petGate`) · با `prefers-reduced-motion` حرکت‌ها می‌ایستند، صداها فقط با انتخاب کاربر.

### ۳.۴ کلیک روی جوجه

**رفتار:**
- انیمیشن `wiggle` `.9s`
- حباب کوتاه با **یک جملهٔ تصادفی** از متن ثابت (زیر)
- **هیچ پاداشی، هیچ امتیازی، هیچ رشدی** (`CAR-01`: وضعیت از دادهٔ معتبر می‌آید، نه از کلیک)
- **صدا هم پاداش نیست**: خرخر و جیک، واکنش‌اند؛ هیچ سنجه‌ای را پر نمی‌کنند

**`[تصمیم]` متن‌های واکنش تصادفی (ثابت، بدون وابستگی به داده):**
| مرحله | متن‌ها |
|---|---|
| تخم | «…تکون خورد!» · «صدای تو را شناخت.» · «منتظر است.» |
| جوجه | «چی چی!» · «خوشحالم که هستی.» · «امروز هم منتظرت بودم.» |

> **`[تصمیم]` محدودیت:** واکنش به کلیک **بی‌نهایت** نیست و **هرگز** به «هر چه بیشتر کلیک کنی بهتر است» تبدیل نمی‌شود. اگر کاربر زیاد کلیک کند، واکنش‌ها پس از چند بار **آرام‌تر** می‌شوند (`[نیازمند راستیآزمایی]` آستانه).

---

## ۴) چهار سنجهٔ مراقبت — `.pmeter` / `.psegs`

| # | ایموجی | نام | زیرنویس | کلید |
|---|---|---|---|---|
| ۱ | 🌾 | ظرف دونه | «با ثبت کارهای روزانه پر می‌شود» | `seed` |
| ۲ | 💧 | ظرف آب | «با لیوان‌های آبِ تو» | `water` |
| ۳ | 🏠 | خونهٔ تمیز | «با ثبت حال ۵ قدمی» | `home` |
| ۴ | 🛁 | وقت حمام | «با ذخیرهٔ ثبت‌ها و تمرین نفس» | `bath` |

```css
.psegs { display:flex; gap:4px }
.psegs i { width:20px; height:9px; border-radius:99px;
           background:var(--ring-track); transition:background .35s }
.psegs i.on { background: linear-gradient(90deg, var(--grad1), var(--grad2)) }
```

**سه بخش** هر سنجه (`3 × 4 = 12` کل).

> ### 🔴 منبع هر سنجه — فقط دادهٔ معتبر
>
> | سنجه | چه چیزی آن را پر می‌کند | ممنوع |
> |---|---|---|
> | دونه | ثبت معتبر یک کار روزانه | کلیک روی چیزی |
> | آب | **ثبت قطعی‌شدهٔ** آب — نه پیش‌نویس | `WTR-10`: پیش‌نویس هیچ پاداشی نمی‌سازد |
> | خونهٔ تمیز | ثبت معتبر حال ۵ قدمی | فقط بازکردن ویزارد |
> | وقت حمام | ثبت ذخیره‌شده + تمرین تنفس تأییدشده | کلیک روی دکمهٔ تنفس |
>
> **همهٔ این‌ها از بک‌اند می‌آیند.** فرانت هیچ‌کدام را افزایش نمی‌دهد.

**`[نیازمند راستیآزمایی — `OPEN-02`..`OPEN-06`]`** موارد باز که **نباید حدس زده شوند**:
تعداد دقیق ثبت لازم برای هر بخش · رفتار در غیبت (کاهش یا ثابت) · سقف پاداش · اثر ثبت با تأخیر · وضعیت آغازین کاربران فعلی سایت.

**نمایش کاهش (اگر تصمیم گرفته شد):** `PD-008` می‌گوید بازگشت با استقبال باشد. پس:
- کاهش هرگز **صفر** نمی‌کند و مرحله را برنمی‌گرداند به تخم.
- کاهش با متن همراه است، نه فقط نوار خالی.
- **ممنوع:** «جوجه‌ات گرسنه مانده» · «به من اهمیت ندادی» · هر جملهٔ سرزنش‌آمیز.

---

## ۵) نوار پیشرفت رشد

```
[درصد مراقبت]  [پیام مرحله]
```

**متن‌های مرحله‌ای (از 7B، تأییدشدهٔ این سند):**

| شرط | تیتر | زیرنویس | پیام |
|---|---|---|---|
| ترک نخورده | «تخم جوما 🥚» | «با مراقبت، به دنیا می‌آید» | «هر قدم تو، یک دونه برای این تخم کوچولوست 🌱» |
| ترک شروع شده | «تخم جوما 🥚» | «دارد ترک می‌خورد! نزدیک است…» | «تخم می‌لرزد! ادامه بده، دارم می‌آیم… 🐣» |
| تولد | «جوجهٔ من 🐣» | «هر روز از من مراقبت کن» | «دونه، آب، خونهٔ تمیز و حمام؛ من با همین‌ها شادم!» |
| رشد کامل | «جوجهٔ من 🐣» | «هر روز از من مراقبت کن» | «چه مراقبتی! حسابی خوشحالم 🎉» |

### 🔴 تولد — تصمیم مهم

**در 7B:** تولد با «مجموع چهار سنجه = ۱۲» رخ می‌داد و پس از تولد، شمارش **صفر** و هر ۱۲ واحد بعدی **دوباره تولد** می‌داد.

**مسائل:** `OPEN-03` می‌گوید «تعداد گام‌های تولد و رشد در نسخهٔ تجاری باز است؛ اعداد A فقط مرجع نمونه‌اند.»

**`[تصمیم]` رفتار فعلی این سند:**

| قاعده | تصمیم |
|---|---|
| آستانهٔ تولد | **بک‌اند تعیین می‌کند.** فرانت عدد ثابت ندارد. |
| بازنشانی پس از تولد | **ممنوع.** جوجه فقط **یک بار** به دنیا می‌آید؛ پس از آن **رشد** می‌کند، نه تولد دوباره. `[تصمیم]` |
| دلیل | تولد دوباره، «معجزه» را به حلقهٔ تکراری تبدیل می‌کند و ارزش احساسی را می‌کشد. |
| مراحل رشد پس از تولد | `[نیازمند راستیآزمایی — OPEN-03]` فعلاً یک مرحلهٔ «جوجه»، با نوار رشد که پر می‌شود. |

**زر «بازنشانی دموی تخم» از 7B حذف می‌شود** — دکمهٔ دموست، نه محصول.

---

## ۶) نام‌گذاری جوجه

**`[تصمیم]`** کاربر می‌تواند **یک بار** برای جوجه‌اش نام بگذارد، بعد از تولد.

```
«حالا که به دنیا آمده، اسمش را چه بگذاریم؟»
[input — حداکثر ۱۶ کاراکتر]
[بله، همین باشد]   [بعداً]
```

| قاعده | مقدار |
|---|---|
| اعتبارسنجی | `۲` تا `۱۶` کاراکتر · فقط حروف و فاصله · بدون محتوای نامناسب (فیلتر بک‌اند) |
| تغییر بعدی | یک بار دیگر مجاز، با متن «مطمئنی؟» `[تصمیم]` |
| اگر نام نگذاشت | «جوجهٔ من» به‌عنوان نام پیش‌فرض |
| نام در خانه نمایش داده می‌شود؟ | **نه.** کارت خانه «جوجهٔ من» می‌ماند. نام فقط در این صفحه. `[تصمیم]` |

**`[نیازمند راستیآزمایی]`** نام جوجه در بک‌اند ذخیره می‌شود یا فقط ترجیح نمایش؟ اگر ذخیره نشود، بین دستگاه‌ها فرق می‌کند.

---

## ۷) پیام جوما در این صفحه

**فقط یک پیام.** جغد `#owl-hi` یا `#owl-cheer` + متن از بک‌اند.

**متن‌ها بر اساس وضعیت:**
| وضعیت | متن |
|---|---|
| تخم در حال رشد | «هر ثبت تو، یک دونه برای تخم است.» |
| نزدیک تولد | «نزدیک است…» |
| بعد از تولد | «ازت خوشش آمده. هر روز سر بزن.» |
| بعد از غیبت | «خوش برگشتی. جوجه منتظرت بود.» — **بدون شمارش روزهای غیبت** |

---

## ۸) تاریخچهٔ مراحل — `[تصمیم]`

بخش تازه‌ای که در 7B نبود و از `۱۰٫۶` نتیجه می‌شود:

```
[فهرست زمانی]
🥚  ۱۴ شهریور  —  تخم گذاشته شد
🥚  ۱۸ شهریور  —  تخم ترک خورد
🐣  ۲۲ شهریور  —  به دنیا آمد
```

**حفظ تاریخچه مستقل از مهر:`PD-008`** — کاربر همیشه می‌بیند چقدر مسیر آمده، حتی اگر سنجه‌ها خالی شوند.

---

## ۹) حالت‌های صفحه

| حالت | رفتار |
|---|---|
| کاربر تازه | تخم، هر چهار سنجه خالی، پیام «هر قدم تو، یک دونه…» — **بدون قفل** |
| بدون برنامه | صفحه فعال است؛ سنجه‌ها پر نمی‌شوند؛ CTA «ساخت برنامه» |
| جوجه گرسنه / سنجهٔ خالی | پیام دعوتی بدون سرزنش |
| بازگشت پس از غیبت بلند | **بدون پیام غیبت.** همان پیام عادی. مهرهای قبلی سالم. |
| آب پیش‌نویس دارد | سنجهٔ آب **عوض نمی‌شود**؛ زیرش متن: «وقتی آب امروزت قطعی شد، اینجا حساب می‌شود.» |
| بک‌اند در دسترس نیست | «وضعیت جوجه الان در دسترس نیست.» + دکمهٔ تلاش دوباره |

---

## ۱۰) قواعد سرخ

| ممنوع | منبع |
|---|---|
| مرگ، بیماری یا فرار جوجه | `PD-008`, `۱۰٫۱۰` |
| شمارش معکوس روزهای غیبت | `۱۰٫۱۰` |
| اعطای پاداش به‌خاطر کلیک | `CAR-01`, `WTR-10` |
| پاداش برای **پیش‌نویس** آب | `WTR-10` |
| تبدیل جوجه به ارزش اصلی محصول | `CAR-04` |
| فشار روانی («جوجه‌ات ناراحت است») | `۱۰٫۱۰` |
| مقایسه با جوجهٔ دیگران | `۱۰٫۱۰` |
| قفل کردن قابلیت‌های اصلی پشت رشد جوجه | `CAR-04` |
| جوجهٔ مرغی زرد یا هر سبک خارج از خانوادهٔ جغد | تصمیم مالک |
| پاداش‌دادن با صدا (خرخر/جیک جای سنجه را بگیرد) | `CAR-01` |
| اشتراک‌گذاری وضعیت جوجه با همراه، بدون تصمیم صریح | `۱۷٫۲` |

---

## ۱۱) پذیرش

- [ ] جوجه هیچ پیام راهنما یا بینشی تولید نمی‌کند
- [ ] کلیک روی جوجه هیچ پاداشی نمی‌دهد
- [ ] سنجهٔ آب با پیش‌نویس پر نمی‌شود
- [ ] در غیبت، هیچ متن سرزنش‌آمیزی وجود ندارد
- [ ] آستانهٔ تولد از بک‌اند می‌آید، نه از عدد ثابت
- [ ] تولد فقط یک بار رخ می‌دهد
- [ ] تاریخچهٔ مراحل حتی با سنجه‌های خالی باقی می‌ماند
- [ ] اگر خالقیت منشوری خراب شد، حالت پوشش دارد
- [ ] جوجه **جوجهٔ جغد** است، نه جوجهٔ مرغ زرد
- [ ] طرح «الف — بدنِ گردِ یکپارچه» پیاده شده است (کاکل بلند یا سبک متفاوت ندارد)
- [ ] رنگ‌های جوجه از خانوادهٔ جغد محصول است (آبی `#5E93C8` · شکم `#F6EBD9` · حلقهٔ طلایی `#E8B64A`)
- [ ] شش حالت (آرام · شاد · خواب · نوازش · کم‌رنگ · خاکستری) از بک‌اند می‌آید
- [ ] حالت‌ها از کلیک ساخته نمی‌شوند؛ لمس بی‌پاداش است
- [ ] صداهای جوجه (جیک · خرخر · خروپف) با کلید مستقل و پیش‌فرض خاموش


---

## 🔴 یافتهٔ بازبینی بک‌اند — دور ۲۰

> **قاعدهٔ قفل:** این بند **افزودنی** است؛ هیچ تصمیم قبلی این فصل بازنویسی نشد. منبع کامل: `docs/handoff/09-backend-gap-order.md`.

**بک‌اند اعلام کرد:** 🔴 **«جوجه و رشدش در کد فعلی نیست.»**

**پیامد تا ساخت (`B5` در `handoff/09`) — با مرحله‌بندی پیشنهادی:**

1. چهار سنجه از دادهٔ موجود · 2) شش حالت با قاعدهٔ غیبت ۳–۵ و ۶+ · 3) سه مرحلهٔ رشد با آستانه در تنظیمات بک‌اند.

**تا آن روز:** صفحهٔ «جوجهٔ من» حذف نمی‌شود ولی در حالت «تخم/به‌زودی» می‌ماند و **هیچ سنجه، حالت یا رشدی جعل نمی‌شود**. تصمیم‌های قفل‌شدهٔ جوجه (نسخهٔ ۱۷) دست‌نخورده‌اند.


---

### به‌روزرسانی دور ۲۱ — جوجه با **سه سنجه** شروع می‌شود `[قطعی]`

**بک‌اند (`N16`) — منبع فعلی هر سنجه:**

| سنجه | منبع در بک‌اند فعلی |
|---|---|
| 🌾 دونه | ثبت عملکرد همان روز |
| 💧 آب | ثبت `ACT002` |
| 🏠 خونهٔ تمیز | بودن رکورد حال امروز |
| 🛁 وقت حمام | **هیچ منبعی ندارد** (`ACT008` فقط یک فعالیت عادی است) |

**تصمیم مالک:** با **سه سنجه** شروع شود؛ سنجهٔ چهارم **نمایش داده نمی‌شود** تا منبع واقعی‌اش (جلسهٔ تمرین تنفس) ساخته شود.
**دربارهٔ آب:** چون پیش‌نویس/قطعی وجود ندارد، **هر ثبتِ موجود «قطعی» حساب می‌شود**؛ پس قاعدهٔ «پیش‌نویس آب پاداش نمی‌سازد» موقتاً معلق می‌ماند و اگر روزی پیش‌نویس خواستیم، از نو طراحی می‌شود.

---

## ✅ اجراشدنی شد — دور ۲۳ `[قطعی]`

> منبع: گزارش بک‌اند روی **تست** (سایت اصلی دست‌نخورده) · صورت‌جلسه: `docs/handoff/15-round-23-backend-status.md`
> **این بند افزودنی است**؛ هیچ تصمیم قفل‌شدهٔ قبلی عوض نشد. فقط آن‌جا که متن‌های قدیمی با واقعیت می‌جنگیدند، **جایگزین** شده.

### الف) سه سنجه، از دادهٔ واقعی — اجرا شد

| سنجه | منبع اجراشده | مخرج (هدف روزانه) |
|---|---|---|
| 🌾 ظرف دونه | هر **ثبت معتبر** = یک دونه | `[پیشنهاد دور ۲۳]` از «تعداد کارهای همان روزِ برنامهٔ کاربر» — اگر بک‌اند نداد، **نوار نگیرد** و فقط عدد بنویسد |
| 💧 ظرف آب | ثبت `ACT002` | **اسنپ‌شات برنامه** (مثلاً «۳ از ۶») — نه عدد ثابت |
| 🏠 خونهٔ تمیز | بودن رکورد حال همان روز | «۱ از ۱» |
| 🛁 وقت حمام | **`available:false`** — نمایش داده نمی‌شود | — |

**`[تصمیم قفل‌شده — دور ۲۳]` قاعدهٔ صادق‌بودن نوار:** نوار هر سنجه **هرگز به بالا گِرد نمی‌شود** · سنجه‌ای که مخرجش از بک‌اند نیاید **نوار نمی‌گیرد** · و در بالای کارت، **به‌جای «X از ۹» و درصد ساختگی، سه عدد واقعی** نشان داده می‌شود.
**`[قطعی]` «صفر جعلی» نداریم:** اگر فعالیت آب در برنامهٔ کاربر نباشد، پیام «**در برنامه‌ات نیست**» می‌آید — نه «۰».

### ب) اعداد جوجه — تأییدشده

| ترک تخم | تولد | کم‌رنگ | خاکستری | جوجهٔ کوچک | نوپا | بالغ |
|---|---|---|---|---|---|---|
| **۶ دونه** | **۱۲ دونه** | **۳ روز بی‌ثبت** | **۶ روز بی‌ثبت** | ۱۲ | ۴۰ | ۹۰ |

محل تغییر: `config/jooje_config.php` — **فقط این یک فایل**. هر عددی که در رابط دیده می‌شود، از بک‌اند می‌آید؛ فرانت هیچ آستانه‌ای را در خودش نگه نمی‌دارد.
**قفل‌شده‌ها که اجرا هم شد:** تولد **یک‌بار** و برگشت‌ناپذیر · جوجه **نمی‌میرد/بیمار نمی‌شود/فرار نمی‌کند** · «غیبت» **هیچ‌جا** شمرده نمی‌شود · «نوازش» **بی‌پاداش** · خط زمانی **پاک نمی‌شود**.

### پ) نام جوجه — تصمیم باز معلق، بسته شد

| قاعده | مقدار قطعی |
|---|---|
| طول | **۲ تا ۱۶ نویسه** |
| چند بار | **یک بار گذاشتن + یک بار عوض‌کردن** (بعد از بار دوم، دکمهٔ تغییر می‌رود) |
| ذخیره | **در بک‌اند** (بین دستگاه‌ها یکی است) |
| دیده‌شدن | **فقط در صفحهٔ «جوجهٔ من»** — نه کارت خانه، نه هم‌مسیر، نه گزارش |
| خالی بمانَد | مجاز — «جوجهٔ من» می‌ماند |

### ت) `[جایگزین متن قدیمی]` — دو چیزی که در سند می‌گفتیم و **دیگر درست نیست**

| متن قدیمی (در همین فصل و فصل ۱۵) | متن درست از دور ۲۳ |
|---|---|
| «آبِ **پیش‌نویس** پاداش نمی‌سازد» (`WTR-10`) | ✅ **حذف** — پیش‌نویس/قطعی وجود ندارد؛ **هر ثبتِ آب «قطعی» حساب می‌شود** (تصمیم دور ۲۱، اجرای دور ۲۳) |
| «پرچم «آب پیش‌نویس دارد» در پاسخ بک‌اند» | ✅ **حذف** — این پرچم در پاسخ وجود ندارد |
| «هر سنجه ۳ بخش — ۳×۴ = ۱۲» | ✅ مخرج **۹** برای سه سنجه؛ عدد هر سنجه از بک‌اند |

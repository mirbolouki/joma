# 7A — دموی شیشهٔ شفاف و دنیای همراه

**وضعیت دریافت:** ناقص — فقط بخش اول (head + CSS + ابتدای JS تا وسط `targ()`).

## چه چیزی از 7A لازم است

| فایل | چرا |
|---|---|
| کل فایل HTML دمو | مرجع بصری مصوب سند برای: جوجهٔ من، حال من، کارهای امروز، رنگ تم شیشه‌ای |
| JS کامل | منطق جوجه، نیازها، آب، تنفس، دفترچه |
| تصاویر (اگر بیرونی است) | سبک پینترستی سند بدون تصویر ساخته نمی‌شود |
| فیلم/گیف حرکت‌ها | انیمیشن‌ها در فایل استاتیک دیده نمی‌شوند |

## آنچه تا الان استخراج شده

خلاصه در `notes.md` همین پوشه:

- رنگ‌های دقیق دو تم (`#1FA8B8`, `#137E8C`, `#1C3247`, `#BFE0F2`, …)
- دستور کامل شیشه (گرادینت مایل + لبهٔ نورانی + هایلایت درونی + `blur(14px) saturate(1.6)`)
- SVG واقعی جغد و جوجه با رنگ‌هایشان و حالت‌هایشان
- موجودی ۲۰+ جزء رابط (`.act-row`, `.opt`, `.pet-stage`, `.insight-pop`, …)
- ۵ پرسش باز که بخش دوم جواب می‌دهد

## نکته

نام فایل مهم نیست؛ فقط همان چیزی که داری را بگذار. اگر کامل است، همان `7a.html` کافی است.
